CREATE TABLE RESERVATION (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1),
  show_id BIGINT,

  PRIMARY KEY (id),

  CONSTRAINT FK_SHOW_ID FOREIGN KEY (show_id) REFERENCES SHOW(id)
);

INSERT INTO RESERVATION VALUES (NULL, 1);