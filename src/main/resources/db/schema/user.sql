CREATE TABLE USER (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1),
  email VARCHAR(254),
  password VARCHAR(254),
  PRIMARY KEY (id)
  --   CONSTRAINT FKK111111 FOREIGN KEY (reservation_id) REFERENCES RESERVATION(id)
);

INSERT INTO USER VALUES (NULL, 'any', 'aaa');
INSERT INTO USER VALUES (NULL, 'any2', 'aaa');