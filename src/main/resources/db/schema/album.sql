CREATE TABLE ALBUM (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1),
  version BIGINT,
  name VARCHAR(254),
  PRIMARY KEY (id)
);

INSERT INTO ALBUM VALUES (NULL, 0, 'album 1');
INSERT INTO ALBUM VALUES (NULL, 0, 'album 2');