CREATE TABLE MOVIE (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1),
  title VARCHAR(255),
  price DECIMAL(12,4),
  PRIMARY KEY (id)
);

INSERT INTO MOVIE VALUES (NULL, 'movie title', 23);